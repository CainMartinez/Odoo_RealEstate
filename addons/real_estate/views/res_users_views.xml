<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Users Form View -->
    <record id="res_users_view_form_inherit_real_estate" model="ir.ui.view">
        <field name="name">res.users.form.inherit.real.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Real Estate Properties" name="real_estate_properties">
                    <field name="property_ids">
                        <list string="Properties">
                            <field name="name"/>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="state"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
